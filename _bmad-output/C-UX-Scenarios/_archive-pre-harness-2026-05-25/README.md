# Archived UX Scenarios — Pre-Harness-Reframe (2026-05-25)

> **These scenarios are retired.** They were authored before the 2026-05-25 harness-model reframe + party-mode stress-test + operator corrections. The product they describe is no longer the product being built.

## What's in here

| Folder | Why retired |
|---|---|
| `01-stefan-crisis-redeployment/` | Built around RAO-card-with-three-tier-confidence grammar + HAFAS dispatch + Mark-executed gesture. The crisis-redeployment *job* survives in the harness model, but the *surface* is now Kanban + Kondukt. RAO card grammar largely retired; cost-impact-one-liner is no longer a defining feature. |
| `02-stefan-cost-aware-triage/` | Same as 01 — RAO card + modification panel + execution log with modification. Cost-impact framing reframes into Kanban detail card + Kondukt skill output provenance citations. |
| `06-lukas-vendor-warranty-dispute/` | Lukas persona retired (one role, two programme hats — merged into Roland persona). Warranty *recovery workflow* descoped from MVP. Warranty *detection* lives via Live Board recurring-failure annotations + Kondukt *Vendor meeting prep* skill. |
| `07-lukas-ota-canary-drift/` | Lukas persona retired. OTA management descoped from MVP (vendor tooling remains canonical). Configuration drift moved to ECM module (quarterly cadence). |

## What replaces them

The three-view harness model (Live Board + Kanban + Kondukt) covers Roland's full work, including the operational work that scenarios 01/02 described. Vendor evidence work (formerly 06/07) is done through Kondukt skills (Vendor meeting prep, Recurring-failure pattern report) + allowlisted internet egress to vendor portals.

See:
- `../00-ux-scenarios.md` — current scenarios index (post-harness)
- `../11-roland-live-board.md` and successors (when authored) — new MVP scenarios
- `../03/04/05/08/09/10` — scenarios that survived harness reframe (revised in place)
- `../_progress/00-design-log.md` — full harness-reframe + operator-correction entries

## Reversibility

These scenarios are preserved on disk in case a retired feature returns to MVP scope. The product brief + PRD identify warranty recovery and OTA management as Phase 1.5 / Vision tier candidates; if either is reinstated, the corresponding archived scenario is a starting point — but **not the spec**, because the surface they describe (RAO card grammar + three-zone Telematics Control Panel) is also retired. Any reinstatement would be a fresh scenario authoring informed by these archives.

## Naming reminder

The archived scenarios use "Stefan" and "Lukas" as personas. Both pseudonyms are retired (2026-05-25): Stefan→Roland (real name now used directly); Lukas retired entirely (one role with Roland, not separate persona). The archived files keep their original names — historical trail.

---

_Archived 2026-05-25 — harness-model reframe + party-mode + operator corrections._
